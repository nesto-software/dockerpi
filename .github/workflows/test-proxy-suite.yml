name: test-proxy-suite
on:
  workflow_dispatch:

jobs:
  test:
    name: Test installation and lld
    runs-on: ubuntu-latest
    container:
      image: node:14.17.0

    steps:
    - uses: actions/checkout@v2

    - name: Install testsuite deps
      run: |
        cd tests && npm install

    - name: Run testsuite
      run: |
        cd tests && npm run test

    services:
      pi:
        image: ghcr.io/${{ github.repository_owner }}/dockerpi:latest
        ports:
          - 5022:5022
        options: pi2
        credentials:
          username: ${{ github.repository_owner }}
          password: ${{ secrets.CR_PAT }}